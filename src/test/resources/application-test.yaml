spring:
  datasource:
    url: jdbc:h2:mem:markethub_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: sa
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  liquibase:
    enabled: false

jwt:
  secret: test-secret-key-test-secret-key-123456
  expiration: 3600000


app:
  admin:
    email: test-admin@example.com
    password: test-password
    first-name: Test
    last-name: Admin
    birth-date: 1990-01-01